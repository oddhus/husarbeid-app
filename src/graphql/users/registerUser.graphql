mutation Register($name: String!, $password: String!) {
  createUser(input: { name: $name, password: $password }) {
    token
    user {
      id
      username
    }
    errors {
      code
      message
    }
  }
}
